function SetPedFlag(object, flag, enabled)
    exports['warp-flags']:SetPedFlag(object, flag, enabled)
end

function HasPedFlag(object, flag)
    return exports['warp-flags']:HasPedFlag(object, flag)
end

function GetPedFlags(object)
    return exports['warp-flags']:GetPedFlags(object)
end

function SetPedFlags(object, flag)
    exports['warp-flags']:SetPedFlags(object, flag)
end

function SetVehicleFlag(vehicle, flag, enabled)
    exports['warp-flags']:SetVehicleFlag(vehicle, flag, enabled)
end

function HasVehicleFlag(vehicle, flag)
    return exports['warp-flags']:HasVehicleFlag(vehicle, flag)
end

function GetVehicleFlags(vehicle)
    return exports['warp-flags']:GetVehicleFlags(vehicle)
end

function SetVehicleFlags(object, flag)
    exports['warp-flags']:SetVehicleFlags(object, flag)
end

function SetObjectFlag(object, flag, enabled)
    exports['warp-flags']:SetObjectFlag(object, flag, enabled)
end

function HasObjectFlag(object, flag)
    return exports['warp-flags']:HasObjectFlag(object, flag)
end

function GetObjectFlags(object)
    return exports['warp-flags']:GetObjectFlags(object)
end

function SetObjectFlags(object, flag)
    exports['warp-flags']:SetObjectFlags(object, flag)
end